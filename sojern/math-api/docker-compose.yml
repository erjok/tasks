version: '3.6'

services:
  app:
    image: math-api
    build:
      context: .
    ports:
      - 3000:80

  test:
    image: math-api
    command: npm test
    environment:
      - TEST_API_URI=http://app
    depends_on:
      - app